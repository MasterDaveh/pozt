import { ScrollClassDirective } from './scroll-class.directive';

describe('ScrollClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollClassDirective();
    expect(directive).toBeTruthy();
  });
});
