import { RollingListHeaderDirective } from './rolling-list-header.directive';

describe('RollingListHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RollingListHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
