import { RollingListDirective } from './rolling-list.directive';

describe('RollingListDirective', () => {
  it('should create an instance', () => {
    const directive = new RollingListDirective();
    expect(directive).toBeTruthy();
  });
});
